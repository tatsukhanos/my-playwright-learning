tests/ — contains all automated test files.

playwright.config.ts — defines test settings like browser, timeouts, and base URL.

package.json — lists project dependencies and scripts.

node_modules/ — contains installed libraries (auto-generated, don’t edit).

package-lock.json — locks dependency versions for consistency.